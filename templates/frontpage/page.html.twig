{% extends 'layout.html.twig' %}

{% block page_title %}{{ page.title|localise_text|page_title }}{{ parent() }}{% endblock %}

{% block breadcrumbs %}
    <li class="breadcrumb-item active" aria-current="page">
        <a href="{{ path('frontpage/index') }}">
            {{ 'Home'|trans }}
        </a>
    </li>
    {% for parent in parents %}
        {% if null === parent %}
            <li class="breadcrumb-item">...</li>
        {% else %}
            <li class="breadcrumb-item">
                <a href="{{ page_path(
                    category: parent.category.getValueNL,
                    categoryEn: parent.category.getValueEN,
                    subCategory: parent.subCategory.getValueNL,
                    subCategoryEn: parent.subCategory.getValueEN,
                    name: parent.name.getValueNL,
                    nameEn: parent.name.getValueEN,
                ) }}">
                    {{ parent.title|localise_text }}
                </a>
            </li>
        {% endif %}
    {% endfor %}
    <li class="breadcrumb-item active">
        {{ page.title|localise_text }}
    </li>
{% endblock %}

{% block contents %}
    <div class="row">
        <div class="col-md-12">
            <h2>{{ page.title|localise_text }}</h2>
        </div>
    </div>
    {{ page.content|localise_text|raw }}
{% endblock %}
