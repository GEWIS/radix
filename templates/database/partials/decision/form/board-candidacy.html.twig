{{ form_start(form, {'action': path('decision_decision_form', {'form': 'board_candidacy'})}) }}
    {{ form_row(form.boardYear) }}
    {{ form_row(form.candidates) }}
    <div class="decision-form-actions">
        {{ form_widget(form.submit, {'attr': {'class': 'btn-gewis-primary'}}) }}
    </div>
{{ form_end(form) }}
