{# Keyed on the case name rather than the value, which is an integer here and says nothing on its own. #}
{% set tone = {
    'Paid': 'success',
    'Created': 'warning',
    'Pending': 'warning',
    'Expired': 'danger',
    'Failed': 'danger',
    'Cancelled': 'secondary',
} %}
<span class="badge badge-{{ tone[state.name] ?? 'secondary' }}">{{ state.getName|trans }}</span>
