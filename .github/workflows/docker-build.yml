name: Docker Image CI

# Only run this action when pushing to main (development release) or on tagged versions (latest release).
on:
    push:
        branches:
            - main
        tags:
            - 'v*.*.*'

# Cancel running jobs that have become stale through updates to the ref (e.g., pushes to a pull request).
concurrency:
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

permissions: {}

env:
    IS_RELEASE: ${{ format('{0}', startsWith(github.ref, 'refs/tags/')) }}

jobs:
    build:
        runs-on: ubuntu-latest
        permissions:
            contents: read
        # One application image, built from the root Dockerfile by target, plus the Matomo sidecar. There is no
        # separate web server image: that is Caddy inside FrankenPHP, and the messenger and scheduler workers run
        # this same image with a different command. The matrix name is the repository name under `web/radix/`.
        strategy:
            matrix:
                image: ['app', 'matomo']
        steps:
            -   name: Checkout
                uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
                with:
                    persist-credentials: false

            -   name: Get short commit SHA
                run: echo "SHORT_COMMIT_SHA=$(git rev-parse --short ${{ github.sha }})" >> $GITHUB_ENV

            -   name: Set up Docker Buildx
                uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0

            -   name: Login to Docker Registry
                uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
                with:
                    registry: ${{ vars.ABC_DOCKER_REGISTRY }}
                    username: ${{ secrets.SVC_GH_ABCWEB_USERNAME }}
                    password: ${{ secrets.SVC_GH_ABCWEB_PWD }}

            -   name: Build and push image
                uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
                with:
                    build-args: GIT_COMMIT=${{ env.SHORT_COMMIT_SHA }}
                    cache-from: type=gha
                    cache-to: type=gha,mode=max
                    context: ${{ matrix.image == 'app' && '.' || format('./docker/{0}', matrix.image) }}
                    file: ${{ matrix.image == 'app' && './Dockerfile' || format('./docker/{0}/Dockerfile', matrix.image) }}
                    target: ${{ matrix.image == 'app' && (env.IS_RELEASE == 'true' && 'radix_app_production' || 'radix_app_development') || '' }}
                    platforms: linux/amd64
                    push: true
                    tags: ${{ format('{0}:{1}', format('{0}/web/radix/{1}', vars.ABC_DOCKER_REGISTRY, matrix.image), env.IS_RELEASE == 'true' && format('{0},{1}:latest', github.ref_name, format('{0}/web/radix/{1}', vars.ABC_DOCKER_REGISTRY, matrix.image)) || 'development') }}
